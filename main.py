"""Application entry point.

Reserved for the future query / RAG side of the system (API server,
chat loop, retrieval CLI, etc.). The ETL ingestion pipeline lives in
ingest.py.
"""


def main():
    raise NotImplementedError(
        "main.py is a placeholder for the future application entry point. "
        "To run the ingestion pipeline, use: uv run python ingest.py"
    )


if __name__ == "__main__":
    main()
